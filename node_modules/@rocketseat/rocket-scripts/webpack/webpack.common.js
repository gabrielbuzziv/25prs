const webpack = require('webpack');
const { realPath, getEnvironmentValues } = require('../utils');

const environmentValues = getEnvironmentValues();

const commonMainConfig = {
  context: __dirname,
  output: {
    filename: '[name].js',
    path: realPath('dist'),
    publicPath: '/',
  },
  target: 'web',
  node: {
    __filename: false,
    __dirname: false,
  },
  module: {
    rules: [
      {
        test: /\.(gif|png|jpe?g|svg)$/i,
        use: [
          {
            loader: 'file-loader',
            options: {
              outputPath: 'static',
            },
          },
        ],
      },
      {
        test: /\.css$/,
        use: ['style-loader/url', 'file-loader'],
      },
    ],
  },
  resolve: {
    mainFields: ['main', 'browser', 'module'],
    extensions: ['.js'],
  },
  plugins: [
    new webpack.DefinePlugin(environmentValues),
  ],
};

module.exports = commonMainConfig;
