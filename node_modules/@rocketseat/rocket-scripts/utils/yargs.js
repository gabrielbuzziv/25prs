const { argv } = require('yargs')
  .boolean('verbose')
  .boolean('typescript')
  .help('h')
  .alias('h', 'help')
  .alias('v', 'verbose')
  .describe('v', 'Output everything')
  .describe('typescript', 'Force typescript configuration to run');

module.exports = argv;
